# 使用便携压缩包发行

Windows 版本以解压即用的压缩包发行，不使用安装器，也不主动把应用数据写入 AppData、注册表、开始菜单或系统服务。配置、账号、历史和日志默认保存在程序目录，媒体目录也默认位于其中，但允许用户改为其他位置并由应用迁移已有离线包。程序只在用户主动检查时从 GitHub Release 获取更新，下载到程序目录并在重启后替换 `app/`，始终保留 `data/` 和媒体目录，不静默更新或打开浏览器；这一选择以较弱的系统集成换取可见、可控和易整体搬移的数据边界。
