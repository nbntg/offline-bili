# 使用 libmpv 播放并从原始弹幕生成 ASS 轨道

播放器通过 PySide6 嵌入 libmpv，不包含浏览器内核；每个离线包保存原始弹幕数据，并依据当前屏蔽、密度、速度和显示区域设置生成可丢弃的 ASS 缓存轨道。由 mpv 的媒体时钟同时驱动视频与弹幕，使暂停、跳转和 0.05× 至 5.00× 倍速下保持同步；相比自行实现弹幕计时和绘制，这一选择牺牲部分逐帧交互自由度，换取成熟解码、字幕渲染和可靠时间轴。
