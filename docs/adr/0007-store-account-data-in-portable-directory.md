# 在便携目录中保存账号数据

哔哩哔哩登录 Cookie 与缓存账号资料保存在程序自身的 `data` 目录，不使用 Windows 凭据管理器或绑定本机账号的加密。这个选择让整个程序文件夹可以搬移，也符合不向系统目录写入数据的约束，但获得该目录的人可能读取并滥用 Cookie；因此发布文档必须明确说明 `data` 包含敏感账号数据，用户不得分享，发行压缩包也绝不携带开发者的 `data` 内容。
